{
  "name": "@openclaw/claude",
  "version": "0.5.0",
  "description": "OpenClaw plugin that delegates Anthropic model turns to a local @zeroaltitude/openclaw-claude-bridge process via codex-shaped JSON-RPC over stdio.",
  "main": "dist/index.js",
  "type": "module",
  "openclaw": {
    "extensions": ["./dist/index.js"]
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.4.0"
  }
}
