{
  "name": "@openclaw/provenance",
  "version": "0.2.1",
  "description": "OpenClaw plugin for content provenance taint tracking and security policy enforcement",
  "main": "dist/index.js",
  "openclaw": {
    "extensions": [
      "./dist/index.js"
    ]
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "typecheck:tests": "tsc -p tsconfig.tests.json",
    "dev": "tsc --watch",
    "test": "vitest run",
    "validate": "npm run typecheck && npm run typecheck:tests && npm run test"
  },
  "devDependencies": {
    "@types/node": "^25.2.1",
    "openclaw": "file:../../openclaw",
    "typescript": "^5.4.0",
    "vitest": "^4.0.18"
  }
}
