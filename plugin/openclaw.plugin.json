{
  "id": "vestige",
  "name": "Vestige Cognitive Memory",
  "version": "0.2.0",
  "description": "Shared cognitive memory for OpenClaw agents powered by Vestige — FSRS-6 spaced repetition, dual-strength memory, and prediction error gating. Includes content provenance taint tracking and security policy enforcement.",
  "author": "BigHat Biosciences",
  "main": "dist/index.js",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "serverUrl": {
        "type": "string",
        "description": "Vestige bridge server URL",
        "default": "http://vestige.internal:8000"
      },
      "authToken": {
        "type": "string",
        "description": "Bearer token for Vestige API authentication"
      },
      "taintPolicy": {
        "type": "object",
        "description": "Default policy mode per taint level. Modes: allow (no restrictions), confirm (block + approval codes), restrict (silently remove, no override), Must be monotonically non-decreasing in strictness.",
        "properties": {
          "system": { "type": "string", "enum": ["allow", "confirm", "restrict"], "default": "allow" },
          "owner": { "type": "string", "enum": ["allow", "confirm", "restrict"], "default": "allow" },
          "local": { "type": "string", "enum": ["allow", "confirm", "restrict"], "default": "allow" },
          "shared": { "type": "string", "enum": ["allow", "confirm", "restrict"], "default": "restrict" },
          "external": { "type": "string", "enum": ["allow", "confirm", "restrict"], "default": "confirm" },
          "untrusted": { "type": "string", "enum": ["allow", "confirm", "restrict"], "default": "confirm" }
        }
      },
      "toolOverrides": {
        "type": "object",
        "description": "Per-tool policy overrides. Key = tool name (lowercase). Value = object mapping taint levels (or '*') to modes. Overrides can only make things STRICTER than the taint-level default. Example: { \"gateway\": { \"local\": \"restrict\" }, \"read\": { \"*\": \"allow\" } }",
        "additionalProperties": {
          "type": "object",
          "properties": {
            "system": { "type": "string", "enum": ["allow", "confirm", "restrict"] },
            "owner": { "type": "string", "enum": ["allow", "confirm", "restrict"] },
            "local": { "type": "string", "enum": ["allow", "confirm", "restrict"] },
            "shared": { "type": "string", "enum": ["allow", "confirm", "restrict"] },
            "external": { "type": "string", "enum": ["allow", "confirm", "restrict"] },
            "untrusted": { "type": "string", "enum": ["allow", "confirm", "restrict"] },
            "*": { "type": "string", "enum": ["allow", "confirm", "restrict"] }
          }
        }
      },
      "approvalTtlSeconds": {
        "type": "number",
        "description": "How long an approval code is valid (default: 60)",
        "default": 60
      },
      "maxIterations": {
        "type": "number",
        "description": "Max agent loop iterations before blocking the turn (default: 10)",
        "default": 10
      }
    }
  },
  "uiHints": {
    "serverUrl": { "label": "Server URL", "placeholder": "https://vestige.bighatbio.me/api" },
    "authToken": { "label": "Auth Token", "sensitive": true }
  }
}
